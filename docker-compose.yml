version: "3.8"

services:
  test-app:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - be
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/data

volumes:
  sqlite_data:

networks:
  be:
    driver: bridge
